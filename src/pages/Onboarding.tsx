interface Props {}

export const OnboardingPage = ({}: Props) => {
  // const onOnboard = () => {};

  return (
    <main>
      <h2>Welcome</h2>
      <div className="flex space-x-2">
        {/* <Input
          type="text"
          placeholder="Type your message..."
          value={globalMessage}
          onChange={(e) => setGlobalMessage(e.target.value)}
        />
        <Button onClick={onOnboard}>Send</Button> */}
      </div>
    </main>
  );
};
